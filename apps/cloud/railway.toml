[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/cloud/Dockerfile"
watchPatterns = ["/apps/cloud/**", "/packages/**", "/Dockerfile", "/package.json", "/bun.lock"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
