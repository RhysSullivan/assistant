{
  "name": "proto-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:executor": "bun run --cwd executor dev",
    "test:executor": "bun run --cwd executor test",
    "typecheck:executor": "bun run --cwd executor typecheck"
  }
}
